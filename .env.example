HOST=127.0.0.1
PORT=3000
DATABASE_PATH=./data/paper-trader.sqlite
LOG_LEVEL=info
# Leave empty for scheduled AI Trade Signal: no Authorization header required.
# Optional on every HOST, including 0.0.0.0.
API_TOKEN=
